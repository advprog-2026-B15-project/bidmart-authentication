name: CD

on:
  workflow_run:
    workflows: [ "CI" ]
    types: [ completed ]
    branches: [ main ]

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build application JAR
        run: ./gradlew build -x test

      - name: Build Docker image
        run: docker build -t bidmart-authentication:${{ github.sha }} -t bidmart-authentication:latest .

      - name: Deploy (placeholder)
        run: |
          echo "=================================================="
          echo "  Deployment step — configure with your target"
          echo "  Options: push to Docker Hub, deploy to cloud,"
          echo "           or trigger a remote deployment pipeline"
          echo "=================================================="
          echo "Image: bidmart-authentication:${{ github.sha }}"
